String projectName = "项目A";
String pattern = "项目A|项目B|.*";
boolean isMathc = Pattern.matches(pattern,projectName);
System.out.println(isMathc);

. 表示出\m之外的单字符
* 表示前面的子表达式(这里是 .),出现任意多次
| 或

所以这里的 "项目A|项目B|.*" 的意图是，去"项目A"中匹配 ，‘项目A’或'项目B'或‘任意词组’  这里加任意词组是不想影响测试环境。


其他正则语法
$ 字符串结尾
^ 字符串开始
() 括号 ，表达一个子表达式
+ 匹配前面的子表达式，一次或者多次
[ 中括号表达式的开始 ？？？？
? 表示前面的子表达式可有可无。 匹配前面的表达式一次或者零次
\ 转义
{ 限定符表达式的开始 ？？？？


限定符
限定符用来指定 子表达式必须要匹配的次数  。有 * ，+，？，{n},{n,},{n,m}六种
? 零次或者1次
+ 一次或者多次
* 任意多次
{n} n次
{n,} 至少n 次
{n,m} n到m 次 .  n<=m
